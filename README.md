# markdown-section-query
`markdown-section-query` is a single-file Markdown section query CLI. It reads one Markdown file, matches recognized sections, and writes a filtered Markdown view to stdout.  This release is CLI-only.
